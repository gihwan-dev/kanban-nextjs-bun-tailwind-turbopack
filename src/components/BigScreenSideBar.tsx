import SideBarTop from "./SideBarTop";

const BigScreenSideBar = () => {
  return (
    <nav className="h-full py-8 px-6 -bg--White hidden md:block box-border">
      <SideBarTop />
    </nav>
  );
};

export default BigScreenSideBar;
