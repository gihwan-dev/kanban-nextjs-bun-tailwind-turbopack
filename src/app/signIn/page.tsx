import { SignInForm } from "@/features/signIn";

const SignInPage = () => {
  return (
    <main className="w-screen h-screen flex flex-col justify-center items-center -bg--light-grey-light-bg">
      <SignInForm />
    </main>
  );
};

export default SignInPage;
